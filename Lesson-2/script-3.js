/*
5. Реализовать основные 4 арифметические операции в виде функций с двумя параметрами. Обязательно использовать оператор return.
*/

let a = 12;
let b = 14;
function sum(a, b) {
    return (a + b);
}
alert('SUM is :' + sum(a, b));

function dif(a, b) {
    return (a - b);
}
alert('DIF is :' + dif(a, b));

function prod(a, b) {
    return (a * b);
}
alert('PROD is :' + prod(a, b));

function div(a, b) {
    return (a / b);
}
alert('DIV is :' + div(a, b));

